import React from 'react';
import {StyleSheet} from 'react-native';
import DefaultLayout from '../../layout/DefaultLayout';

const styles = StyleSheet.create({
  container: {
    ...DefaultLayout.screenContainer,
  },
});

export default styles;
